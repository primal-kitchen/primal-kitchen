events {}
http {
	server {
		listen	80;
	}

	server {
		server_name primalkitchen;

		location / {
			proxy_pass	http://frontend:3000;
		}
	}

	server {
		server_name api.primalkitchen;

		location / {
			proxy_pass	http://mock-api:80;
		}
	}
}
